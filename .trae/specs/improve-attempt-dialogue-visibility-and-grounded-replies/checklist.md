- [x] `attempt1` 页面默认不展示历史对话记录，用户主动点击后才展开
- [x] `attempt2` 页面默认不展示历史对话记录，用户主动点击后才展开
- [x] 历史记录默认隐藏后，实时字幕仍按原流程正常显示
- [x] 前端日志可区分本轮最终文本、实际发送文本、上传完成、等待模型回复等关键阶段
- [x] 后端日志可明确标识本轮用户文本来源是 Flash ASR、Whisper、Web Speech 或其他回退
- [x] 后端日志可追踪送入模型的最终 `user_text` 与 `conversation_history`
- [x] 后端日志可追踪模型原始回复与回传前端的最终回复
- [x] 对正常可识别输入，系统不再仅返回 `"Let me check that for you"` 一类泛化句式
- [x] 对正常可识别输入，系统不再仅返回 `"I didn't catch that"` 一类识别失败模板
- [x] 对"具体问题 + 当前主题"输入，AI 回复能直接回应问题点
- [x] 连续多轮对话时，AI 回复能跟随最新用户消息变化
- [x] 若仍触发兜底回复，日志能够说明触发原因并可定位到链路阶段
